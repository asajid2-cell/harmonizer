"""
Reinforcement-learning scaffolding package.

This package will eventually store helpers for logging jump events,
generating labelled snippets, and training preference models.  For now
it only exposes lightweight storage utilities used by the API layer.
"""
