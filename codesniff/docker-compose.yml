version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - CODESCOPE_STORAGE_DIR=/app/storage
      - CODESNIFF_WEB_RUNNER_ENABLED=${CODESNIFF_WEB_RUNNER_ENABLED:-1}
    volumes:
      - ./backend:/app
      - storage_data:/app/storage
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  worker:
    profiles:
      - worker
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - CODESCOPE_STORAGE_DIR=/app/storage
      - CODESNIFF_MAX_ACTIVE_REPOS=${CODESNIFF_MAX_ACTIVE_REPOS:-1}
    volumes:
      - ./backend:/app
      - storage_data:/app/storage
    command: python scripts/run_worker.py

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:8000
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev
    depends_on:
      - backend

volumes:
  storage_data:
