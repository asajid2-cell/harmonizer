# RL Models

This directory is reserved for small, reviewed RL policy metadata that should ship with the public demo.

Large generated training outputs and local runtime data belong under ignored runtime paths such as `backend/data/rl/`. Do not commit local SQLite databases, snippet files, raw event logs, or generated checkpoints.
